/** Initial recommendation cards shown on the results page. */
export const INITIAL_VISIBLE_RECOMMENDATIONS = 6;

/** Additional cards revealed each time "Load More" is clicked. */
export const LOAD_MORE_RECOMMENDATIONS_BATCH = 6;

/** Stagger delay between recommendation card entrance animations (ms). */
export const RECOMMENDATION_STAGGER_MS = 120;
